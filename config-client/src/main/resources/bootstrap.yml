spring:
  application:
    name: a-bootiful-client
  cloud:
    config:
      uri: http://localhost:8888
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
management:
  security:
    enabled: false